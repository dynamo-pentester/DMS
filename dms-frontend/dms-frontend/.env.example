# Base URL of the Driver.API backend (no trailing slash).
# In dev, requests to /api are proxied by vite.config.ts, so this can stay as /api.
# In production builds, point this at the deployed API origin, e.g. https://api.yourcompany.com
VITE_API_BASE_URL=/api
